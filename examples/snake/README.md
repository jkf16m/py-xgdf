# Snake test fixture

A small Python/Pygame Snake game used to exercise the deterministic gq flow.

```bash
python -m pip install pygame
python examples/snake/snake.py
```

Move with arrow keys or WASD. The game ends on wall or self collision.
